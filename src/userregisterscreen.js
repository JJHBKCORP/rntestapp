import React from 'react';
import {View, Button, StyleSheet} from 'react-native';

export default UserRegisterScreen = props => {
  return <View style={styles.container}> </View>;
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
  },
});
